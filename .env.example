# Optional. When set, "Ask this meeting" / "Ask across meetings" send the
# locally retrieved context to Claude (claude-opus-5) to phrase the answer.
# Without it, answers come from the built-in deterministic retrieval engine.
# Server-side only — never exposed to the browser.
ANTHROPIC_API_KEY=
