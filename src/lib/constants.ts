/**
 * The workspace is single-user (there is no sign-in), so "me" is a fixed person
 * row in the database. Everything else about the workspace comes from Postgres.
 */
export const CURRENT_USER_ID = "hammad";
export const COMPANY = "Northstack";
export const PRODUCT_NAME = "Parley";
